#Basic I/O, operations and conditions

#Personal Finance Helper
income = int(input("Enter your income: "))
expenses = int(input("Enter your expenses: "))
savings_goals = int(input("Enter your savings goal that you would like to achieve: "))
months = int(input("Enter the time period you would like to achieve this in (in months): "))

remaining_balance = income - expenses

if income < expenses:
    print("Your income cannot be less than expenses. If so you cannnot save any money.")
else:
    total_savings = remaining_balance * months

    goal_Reach = savings_goals / remaining_balance
    print(f"YOur remaining balance is {remaining_balance} rupess")
    print(f"Your total savings are {total_savings} rupess")
    print(f"Time take to reach your goal is {round(goal_Reach,2)} months")

#Travel time and cost estimator
distance = float(input("Enter the distance in kilometers: "))
mileage = float(input("Enter the mileage of the vehicle: "))
fuel_price = float(input("Enter the fuel price per liter: "))
speed = float(input("Enter the speed of the vehicle in kilometers: "))

fuel_needed = distance / mileage
cost = fuel_needed * fuel_price
time_period = distance / speed

print(f"Fuel required is {fuel_needed:.2f} liters")
print(f"Total cost needed for the trip is {cost:.2f} rupees")
print(f"Total time needed to reach your destination is {time_period:.2f} hours")

#Fitness Progress calculator
steps_walked = int(input("Enter the total steps walked (in meters): "))
calories_per_km = int(input("Enter the calories burned per kilometer: "))
weight = float(input("Enter your weight in kgs: "))
speed = int(input("Enter the speed you were walking at: "))

distance = steps_walked / 1000
calories_burned = distance * calories_per_km
time_spent = distance / speed

print(f"Total distance walked is {distance:.1f} kilometers")
print(f"Total calories burned are {calories_burned:.1f} kcal")
print(f"Total time spent exercising is {time_spent:.2f} hours")

#Electricity bill manager

units_consumed = float(input("Enter the total units that were consumed for 3 months: "))

average = units_consumed / 3
print(f"Units used for one month are {average:.1f} units")
time.sleep(2)

if average <= 100:
    print("The cost per unit is $5")
    bill = average * 5
elif average >= 101 and average < 200:
    print("The cost per unit is $7")
    bill = average * 7
else:
    print("The cost per unit is $10")
    bill = average * 10

time.sleep(2)
bill_for_3months = bill * 3
print(f"The bill for three months with the given units is {bill_for_3months:.0f} dollars")

#Student marks analyser
subject_1 = int(input("Enter the marks of subject 1: "))
subject_2 = int(input("Enter the marks of subject 2: "))
subject_3 = int(input("Enter the marks of subject 3: "))
subject_4 = int(input("Enter the marks of subject 4: "))
subject_5 = int(input("Enter the marks of subject 5: "))
total_marks = subject_1 + subject_2 + subject_3 + subject_4 + subject_5
average = total_marks / 5
if average >= 90:
    print("A grade")
elif average >= 80:
    print("B grade")
elif average >=70:
    print("C grade")
elif average >=60:
    print("D grade")
elif average >=50:
    print("E grade")
else:
    print("F grade")


# Temperature Converter

temperature_celsius = float(input("Enter the temperature in Celsius: "))

fahrenheit = (temperature_celsius * 9/5) + 32

kelvin = temperature_celsius + 273.15

print(f"Temperature in Celsius: {temperature_celsius:.2f}°C")
print(f"Temperature in Fahrenheit: {fahrenheit:.2f}°F")
print(f"Temperature in Kelvin: {kelvin:.2f}K")

#Time conversion

total_minutes = int(input("Enter the total time in minutes: "))
hours = total_minutes // 60
minutes = total_minutes % 60

print(f"Total time is {hours} hrs and {minutes} mins")

total_seconds = int(input("Enter hte total time in seconds: "))
hours = total_seconds // 3600
remaining_seconds = total_seconds % 3600
minutes = remaining_seconds //60
seconds = remaining_seconds % 60
print(f"Total time is {hours} hrs {minutes} mins and {seconds} secs")

#Area and volume calculator 
import math

while True:
    shape = input("Enter the shape you would like calculate the area, perimeter or volume of (rectangle, square, triangle, circle, cylinder, sphere): ").lower().strip()
    
    if shape == "rectangle":
        length = int(input("Enter the length of the rectangle: "))
        width = int(input("Enter the width of the rectangle: "))
        area = length * width
        perimeter = 2 * (length + width)
        print(f"Area of the rectangle is {area}")
        print(f"Perimeter of the rectange is {perimeter}")
        break

    elif shape == "square":
        side = int(input("Enter the side of the square: " ))
        perimeter = 4 * side
        area = side ** 2
        print(f"Area of square is {area}")
        print(f"Perimeter of the square is {perimeter}")
        break

    elif shape == "triangle":
        breadth = int(input("Enter the breadth of the triangle: "))
        height = int(input("Enter the height of the triangle: "))
        a = int(input("Enter the a side of triangle: "))
        b = int(input("Enter the b side of triangle: "))
        c = int(input("Enter the c side of triangle: "))
        area = 1/2 * breadth * height
        perimeter = a + b + c
        print(f"Area of the triangle is {area}")
        print(f"Perimeter of the triangle is {perimeter}")
        break

    elif shape == "cylinder":
        radius = int(input("Enter the radius of the cylinder: "))
        height = int(input("Enter the height of the cylinder: "))
        volume = math.pi * radius**2 * height
        area = 2 * math.pi * radius * height + 2 * math.pi * radius ** 2
        print(f"Area of the cylinder is {area}")
        print(f"Volume of the cylinder is {volume}")
        break

    elif shape == "circle":
        radius = int(input("Enter the radius of the circle:"))
        area = math.pi * radius**2
        circumference = 2 * math.pi * radius
        print(f"Area of the circle is {area}")
        print(f"Circumference of the circle is {circumference}")
        break

    elif shape == "sphere":
        radius = int(input("Enter the radius of the sphere: "))
        volume = 4/3 * math.pi * radius**3
        area = 4 * math.pi * radius** 2
        print(f"Volume of the sphere is {volume}")
        print(f"Area of the sphere is {area}")
        break

    else:
        print("Invalid shape given. Please enter the shape from the given list.")
        continue

#Health Stat checker
weight = float(input("Enter your weight in kgs: "))
height = float(input("Enter your height in meters: "))

bmi = weight / (height ** 2)
print(f"Your BMI is {bmi:.2f}")

if bmi < 18.5:
    print("Underweight")
elif bmi < 25:
    print("Normal weight")
elif bmi < 30:
    print("Overweight")
else:
    print("Obese")

#Basic Looping systems

#Basic ATM 

import time

while True:
    pin = input("Enter your four digit pin: ").strip()
    if pin == "":
        continue
    elif pin == "8104":
        time.sleep(2)
        print("Access Granted.\n")
        break
    else:
        print("Incorrect pin. Please try again.")

time.sleep(2)
print("What would you like to choose: \n" 
"1. Deposit \n"
"2. Withdraw \n" 
"3. Balance \n" 
"4. Exit")

balance = 2000

while True:

    option = input("Enter your choice: ")

    if option == "1":
        amount = int(input("Enter the amount you would like to deposit: "))
        balance += amount 
        print(f"Your balance is : {balance}")

    elif option == "2":
        amount = int(input("Enter the amount you would like to withdraw: "))
        if balance < amount:
            print("Insufficient funds to withdraw. Please add more funds.")
        else:
            balance -= amount
            print(f"Your balance is: {balance}")

    elif option == "3":
        print(f"Your current balance is {balance}.")

    elif option == "4":
        print("Thank you for using our bank.")
        break
    
    else:
        print("Invalid option chosen. Please try choosing again.")

#Number guessing game

import random 

random_integer = random.randint(1,100)
while True:
    guess = int(input("Enter your guess for the number: "))
    if guess > random_integer:
        print("Number too high.")
        continue
    elif guess < random_integer:
        print("Number too low.")
        continue
    elif guess == random_integer:
        print("Correct number.")
        break

#Multiplication table:

while True:
    choice = input("Do you want to print multiple tables? (yes/no): ").strip().lower()

    if choice == "yes":
        start = int(input("Enter the starting table: "))
        end = int(input("Enter the ending table: "))

        for table in range(start, end + 1):
            print(f"Multiplication Table for {table}:")
            for j in range(1, 11):
                print(f"{table} x {j} = {table * j}")

    elif choice == "no":
        table = int(input("Enter the table you would like: "))
        print(f"Multiplication Table for {table}:")
        for j in range(1, 11):
            n = table * j
            print(n)

    else:
        print("Invalid choice. Please enter 'yes' or 'no'.")
        continue

    again = input("\nDo you want to try again? (yes/no): ").strip().lower()
    if again != "yes":
        print("Goodbye!")
        break

# Student Attendance System

total_students = int(input("Enter number of students: "))

present_count = 0
absent_count = 0

for i in range(1, total_students + 1):
    name = input(f"Enter student {i} name: ")
    status = input(f"Is {name} present (yes/no)? ").lower().strip()

    if status == "yes":
        present_count += 1
    elif status == "no":
        absent_count += 1
    else:
        print("Invalid input, marking as absent by default.")
        absent_count += 1

print("\nAttendance Report:")
print(f"Total Students: {total_students}")
print(f"Present: {present_count}")
print(f"Absent: {absent_count}")

#FizzBuzz Calc

import time

n = int(input("How many numbers to check: "))

fizz_counter = 0
buzz_counter = 0
fizzbuzz_counter = 0

for i in range(1, n+1):
    if i % 3 == 0 and i % 5 == 0:
        fizzbuzz_counter += 1
        print("FizzBuzz", end=" | ")
    elif i % 3 == 0:
        fizz_counter += 1
        print("Fizz", end=" | ")
    elif i % 5 == 0:
        buzz_counter += 1
        print("Buzz", end=" | ")
    else:
        print(i, end=" | ")


time.sleep(2)
print("\nCount Report:")
print(f"Fizz Counter: {fizz_counter}")
print(f"Buzz Counter: {buzz_counter}")
print(f"FizzBuzz Counter: {fizzbuzz_counter}")

#Menu based calculator

import time

while True:
    option = int(input("List of options you have:\n"
                       "1. Add\n"
                       "2. Subtract\n"
                       "3. Multiply\n"
                       "4. Divide\n"
                       "5. Exit\n"
                       "Enter your choice: "))

    if option == 1:
        a = int(input("Enter the first value: "))
        b = int(input("Enter the second value: "))
        print(f"Result: {a + b}")

    elif option == 2:
        a = int(input("Enter the first value: "))
        b = int(input("Enter the second value: "))
        print(f"Result: {a - b}")

    elif option == 3:
        a = int(input("Enter the first value: "))
        b = int(input("Enter the second value: "))
        print(f"Result: {a * b}")

    elif option == 4:
        a = int(input("Enter the first value: "))
        b = int(input("Enter the second value: "))
        try:
            print(f"Result: {a / b}")
        except ZeroDivisionError:
            print("Cannot divide by zero!")

    elif option == 5:
        print("Goodbye.")
        break

    else:
        print("Invalid option. Please choose again.")

    time.sleep(1)

#More loops and conditions

#Library late fee calculator
n = int(input("Enter how many books did you borrow? "))

total_fine = 0

for i in range(1,n+1):
    time = int(input(f"For how many days was this book {i} late by? "))
    
    if time <= 5:
        fine = time * 2
        print(f"Book {i}: Fine is $2 per day -> ${fine}")
        
    elif time >= 6 and time <= 10:
        fine = time * 5
        print(f"Book {i}: Fine is $5 per day -> ${fine}")
        
    elif time > 10:
        fine = time * 10
        print(f"Book {i}: Fine is $10 per day -> ${fine}")
        
    else:
        fine = 0
        print("No fine.")

    total_fine += fine

print(f"\nTotal fine is {total_fine} dollars.")

if total_fine > 500:
    print("Your account has been blocked until you pay the fine.")
else:
    print("You can still borrow books.")

#Bus ticketing system

import time

total_bus_seats = int(input("Enter the total bus seats available: "))
available_seats = total_bus_seats

while True:
    print("\n--- Bus Ticketing System ---")
    print("1. Book a ticket")
    print("2. Cancel a ticket")
    print("3. Check availability")
    print("4. Exit")

    choice = input("Enter your choice: ").strip()

    if choice == "1":
        name = input("Enter passenger name: ")
        number_of_seats = int(input("Enter the number of seats you would like to book: "))
        if available_seats < number_of_seats:
            print("Not enough seats available.")
        else:
            available_seats -= number_of_seats
            print(f"Booking confirmed for {name}. Seats booked: {number_of_seats}")

    elif choice == "2":
        seats_to_cancel = int(input("Enter the number of seats you would like to cancel: "))
        if available_seats + seats_to_cancel <= total_bus_seats:
            available_seats += seats_to_cancel
            print(f"Seat cancellation confirmed. Seats cancelled: {seats_to_cancel}")
        else:
            print("Invalid cancel request (exceeds total seats).")

    elif choice == "3":
        print(f"Total available seats: {available_seats}/{total_bus_seats}")

    elif choice == "4":
        print("Thank you for using our bus service.")
        break

    else:
        print("Invalid choice. Please try again.")

    time.sleep(2)

#Ticket Booking System
import time

total_seats = int(input("Enter total available seats: "))
available_seats = total_seats
revenue = 0
ticket_price = {"normal": 150, "vip": 300}

while True:
    print("\n--- Cinema Booking System ---")
    print("1. Book a ticket")
    print("2. Cancel a ticket")
    print("3. Check seats")
    print("4. Check revenue")
    print("5. Exit")

    choice = input("Enter your choice: ").strip()

    if choice == "1":
        seat_type = input("Seat type (normal/vip): ").strip().lower()

        if seat_type not in ticket_price:
            print("Invalid seat type. Choose 'normal' or 'vip'")
            continue

        seats_to_book = int(input("How many tickets would you like to book? "))
        if seats_to_book > available_seats:
            print("Not enough seats available.")
        else:
            available_seats -= seats_to_book
            cost = seats_to_book * ticket_price[seat_type]
            revenue += cost

            print(f"Booking confirmed! {seats_to_book} {seat_type} seats booked.")
            print(f"Total cost: ${cost}")

    elif choice == "2":
        seat_type = input("Which seat type do you want to cancel (normal/vip): ").strip().lower()

        if seat_type not in ticket_price:
            print("Invalid seat type.")
            continue

        seats_to_cancel = int(input("How many seats to cancel? "))

        if seats_to_cancel > (total_seats - available_seats):
            print("You cannot cancel more than the seats already booked.")
        else:
            available_seats += seats_to_cancel
            refund = seats_to_cancel * ticket_price[seat_type]
            revenue -= refund
            print(f"{seats_to_cancel} {seat_type} seats cancelled. Refund: ${refund}")

    elif choice == "3":
        print(f"Available seats: {available_seats}/{total_seats}")

    elif choice == "4":
        print(f"Total revenue: ${revenue}")

    elif choice == "5":
        print("Thank you for using the cinema booking system!")
        break

    else:
        print("Invalid choice. Try again.")

    time.sleep(1)

#Some more bigger mini projects combining loops, conditionals and operations

#Parking Lot System

import time

cars_slots = int(input("Enter the number of cars slots available: "))
bike_slots = int(input("Enter the number of bike slots available: "))

available_cars = cars_slots
available_bikes = bike_slots

while True:

    print("\n---Parking Lot Management---" \
    "\n1. Park a vehicle" \
    "\n2. Remove a vehicle" \
    "\n3. Check availability" \
    "\n4. Exit")

    choice = input("Enter your choice: ").strip()

    if choice == "1":
        vehicle_type = input("Enter the vehicle type (car/bike): ").strip().lower()
        if vehicle_type == "car":
            if available_cars <= 0:
                print("No space available.")
            else:
                available_cars -= 1
                print("Car parked successfully. Please proceed to find the available slot.")
        elif vehicle_type == "bike":
            if available_bikes <= 0:
                print("No space available.")
            else:
                available_bikes -= 1
                print("Bike parked succesfully. Please proceed to find the available slot.")        
        else:
            print("Invalid car type")
        time.sleep(2)

    elif choice == "2":
        vehicle_type = input("Enter the vehicle type (car/bike): ").strip().lower()
        if vehicle_type == "car":
            if available_cars < cars_slots:
                hours_parked = int(input("How many hours was the car parked for: "))
                available_cars += 1
                cost = hours_parked * 50
                print(f"Cost of parking the car for {hours_parked} hrs is {cost} INR")
            else:
                print("No car is currently parked to remove.")
        elif vehicle_type == "bike":
            if available_bikes < bike_slots:
                hours_parked = int(input("How many hours was the car parked for: "))
                available_bikes += 1
                cost = hours_parked * 20
                print(f"Cost of parking the bike for {hours_parked} hrs is {cost} INR")
            else:
                print("No bike is currently parked to remove")
        else:
            print("Invalid vehicle type.")
        time.sleep(2)

    elif choice == "3":
        vehicle_type = input("Enter the vehicle type (car/bike): ").strip().lower()
        if vehicle_type == "car":
            print(f"The available slots for car parking is {available_cars}")
        elif vehicle_type == "bike":
            print(f"The available slots for bike parking is {available_bikes}")
        time.sleep(2)

    elif choice == "4":
        print("Thank you for parking your car at our parking lot. Goodbye!")
        break

    else:
        print("Invalid Choice. Please choose the correct option.")

#Weekend brushups (single loops and conditionals) 

#Quiz game

import time

print("---Welcome to the Quiz game---")

time.sleep(2)

total_score = 5

while True:
    score = 0
    
    print("\n1. Who won the 2021 Formula one world championship? " \
    "A. Max Verstappen B. Charles Leclerc C. Lewis Hamilton D. Fernando Alonso")
    option = input("Enter your choice: ").strip().lower()
    if option == "a":
        score += 1
        print("Correct answer. Well done!")
    else:
        print("Incorrect answer. Max verstappen won the 2021 WDC.")
    
    time.sleep(2)

    print("\n2. Who has the most WDC in formula one? " \
    "A. Max Verstappen B. Michael Schumacher C. Lewis Hamilton D. Fernando Alonso")
    option = input("Enter your choice: ").strip().lower()
    if option == "b" or option == "c":
        score += 1
        print("Correct answer. Well done!")
    else:
        print("Incorrect answer. Michael Schumacher and Lewis Hamilton hold the join record for the most WDC's")

    time.sleep(2)

    print("\n3. Who won the 2016 spanish grand prix in formula one? " \
    "A. Max Verstappen B. Nico Rosberg C. Lewis Hamilton D. Daniel Riccardo")
    option = input("Enter your choice: ").strip().lower()
    if option == "a":
        score += 1
        print("Correct answer. Well done!")
    else:
        print("Incorrect answer. Max verstappen won the 2016 spanish grand prix.")

    time.sleep(2)

    print("\n4. Which team in formula one won the 2007 constructors championship? " \
    "A. McLaren B. Renault C. Redbull D. Ferrari")
    option = input("Enter your choice: ").strip().lower()
    if option == "d":
        score += 1
        print("Correct answer. Well done!")
    else:
        print("Incorrect answer. Ferrari won the 2007 WCC after Mclaren were disqualified after found guilty for cheating")

    time.sleep(2)

    print("\n5. What is the track name for the belgium grand prix? " \
    "A. Spa Francorshamps B. Emilia-Romagna C. Silverstone D. Suzuka")
    option = input("Enter your choice: ").strip().lower()
    if option == "a":
        score += 1
        print("Correct answer. Well done!")
    else:
        print("Incorrect answer. The track name for the belgium grand prix is known as Spa Francorshamps.")

    time.sleep(2)
    print(f"\n--Your scorecard for the results of your quiz game are - " \
    f"\n Score: {score}/{total_score}")
    if score == 5:
        print("Excellent job!")
    elif score >= 3:
        print("Good try!")
    else:
        print("Try again!")

    time.sleep(2)
    
    play_again = input("\nDo you want to play the game again (yes/no)? ").strip().lower()
    if play_again == "no":
        print("Thanks for playing the quiz game. Goodbye!")
        break

#Hotel Room Booking System
import time

#Initialize room counts 

single_rooms = 5
double_rooms = 5
suite_rooms = 2

#Prices

single_rooms_price = 1000
double_rooms_price = 2000
suite_rooms_price = 5000

while True:
    print("---Hotel Room Booking System---" \
    "\n1. Book a room" \
    "\n2. Check out" \
    "\n3. Show available rooms" \
    "\n4. Exit")

    choice = input("Enter your choice: ").strip().lower()
    time.sleep(2)
    if choice == "1":
        name = input("Enter your name: ").strip().title()
        time.sleep(1)
        room_type = input("Enter the room type you would like to check in (single/double/suite): ").strip().lower()
        time.sleep(1)
        if room_type == "single":
            if single_rooms <= 0:
                print("Single bedroom rooms aren't available. Sorry for the inconvenience.")
            else:
                single_rooms -= 1
                print(f"Thank you for booking a single bedroom {name}.")
        elif room_type == "double":
            if double_rooms <= 0:
                print("Double bedroom rooms aren't available. Sorry for the inconvenience.")
            else:
                double_rooms -= 1
                cost = double_rooms_price * double_rooms
                print(f"Thank you for booking a double bedroom {name}.")
        elif room_type == "suite":
            if suite_rooms <= 0:
                print("Suite bedroom rooms aren't available. Sorry for the inconvenience.")
            else:
                suite_rooms -= 1
                cost = suite_rooms_price * suite_rooms
                print(f"Thank you for booking a suite bedroom {name} .")
        else:
            print("Invalid room type")
            
    elif choice == "2":
        room_type = input("What room are you checking out from (single/double/suite)? ").strip().lower()
        time.sleep(1)
        if room_type == "single":
            if single_rooms < 5:
                single_rooms += 1
                print(f"Your bill is {single_rooms_price} INR. Thank you for staying!")
            else:
                print("No record found")
        elif room_type == "double":
            if double_rooms < 5:
                double_rooms += 1
                print(f"Your bill is {double_rooms_price} INR. Thank you for staying!")
            else:
                print("No record found")
        elif room_type == "suite":
            if suite_rooms < 5:
                suite_rooms += 1
                print(f"Your bill is {suite_rooms_price} INR. Thank you for staying!")
            else:
                print("No record found")
        else:
            print("Invalid room type")
                
    elif choice == "3":
        print(f"\nAvaialable rooms are: " \
        f"\n Single Rooms: {single_rooms}" \
        f"\n Double Rooms: {double_rooms}" \
        f"\n Suite Rooms: {suite_rooms}")

    elif choice == "4":
        print("Thank you for using our hotel booking service. Goodbye!")
        break

    else:
        print("Invalid choice. Try again")

#Food ordering menu

import time

# Menu items and prices
Pizza = 150
Burger = 100
Pasta = 120
Cold_Drink = 50

pizza_count = 0
burger_count = 0
pasta_count = 0
cold_drink_count = 0
total_cost = 0

while True:
    print("\n---Rexy's Joint Menu---"
          "\n1. Pizza - ₹150"
          "\n2. Burger - ₹100"
          "\n3. Pasta - ₹120"
          "\n4. Cold Drink - ₹50")

    choice = input("\nWhat would you like to have? ").strip().lower()
    quantity = int(input("How many would you like? "))

    if choice == "pizza":
        pizza_count += quantity
        total_cost += Pizza * quantity
    elif choice == "burger":
        burger_count += quantity
        total_cost += Burger * quantity
    elif choice == "pasta":
        pasta_count += quantity
        total_cost += Pasta * quantity
    elif choice in ["cold_drink", "cold drink"]:
        cold_drink_count += quantity
        total_cost += Cold_Drink * quantity
    else:
        print("Invalid Choice. Please choose correct option.")
        continue

    order_more = input("\nWould you like to order more? (yes/no): ").strip().lower()
    if order_more in ["yes", "y"]:
        continue
    elif order_more in ["no", "n"]:
        break
    else:
        print("Invalid choice, assuming you want to continue.")
        continue

time.sleep(2)

print("\n--- Bill Summary ---")
print(f"Pizza: {pizza_count} x {Pizza} = {pizza_count * Pizza}")
print(f"Burger: {burger_count} x {Burger} = {burger_count * Burger}")
print(f"Pasta: {pasta_count} x {Pasta} = {pasta_count * Pasta}")
print(f"Cold Drink: {cold_drink_count} x {Cold_Drink} = {cold_drink_count * Cold_Drink}")
print(f"\nTotal Bill: ₹{total_cost}")

if total_cost > 500:
    discount = 0.1 * total_cost
    final_cost = total_cost - discount
    print(f"Discount Applied: ₹{discount}")
    print(f"Final Bill After Discount: ₹{final_cost}")

time.sleep(1)
print("\nThank you for ordering at Rexy's Joint. Goodbye!")

#Basic List Problems

#Grocery Store

import time

Grocery_List = []

while True:
    print("\n---Grocery List Options---" \
    "\n1. Add Item" \
    "\n2. Remove Item" \
    "\n3. Show List" \
    "\n4. Exit")
    
    choice = input("\nEnter your choice: ").strip()

    if choice == "1":
        item = input("What item would you like to add to your list? ").title()
        print("Item successfully added to the list.")
        Grocery_List.append(item)
        pass
        time.sleep(2)
    elif choice == "2":
        item = input("What item would you like to remove from the list? ").title()
        if item not in Grocery_List:
            print("Item not in the list.")
        else:
            Grocery_List.remove(item)
            print("Item successfully removed from the list.")
        pass
        time.sleep(2)
    elif choice == "3":
        if Grocery_List == 0:
            print("Grocery List is empty.")
        else:
            print("\nGrocery List is:")
            for i in range(len(Grocery_List)):
                print(i+1, Grocery_List[i], sep=". ")
        pass
        time.sleep(2)
    elif choice == "4":
        print("Thank you for shopping at our store!")
        break
    else:
        print("Invalid option. Please choose again.")

#To do task manager

import time

tasks = []

while True:
    print("\n--- To-Do List Options ---" \
    "\n1. Add Task" \
    "\n2. Mark Task as Done" \
    "\n3. Show Tasks" \
    "\n4. Exit")

    choice = input("\nEnter your choice: ").strip()

    if choice == "1":
        task = input("Enter a new task: ").capitalize()
        tasks.append({"task": task, "done": False})
        print("Task added.")
        time.sleep(1)

    elif choice == "2":
        if not tasks:
            print("No tasks available.")
        else:
            for i, t in enumerate(tasks):
                status = "✓" if t["done"] else "✗"
                print(f"{i+1}. {t['task']} [{status}]")
            index = int(input("Enter task number to mark as done: ")) - 1
            if 0 <= index < len(tasks):
                tasks[index]["done"] = True
                print("Task marked as done.")
            else:
                print("Invalid task number.")
        time.sleep(1)

    elif choice == "3":
        if not tasks:
            print("No tasks yet.")
        else:
            print("\n--- To-Do List ---")
            for i, t in enumerate(tasks):
                status = "✓" if t["done"] else "✗"
                print(f"{i+1}. {t['task']} [{status}]")
        time.sleep(1)

    elif choice == "4":
        print("Goodbye!")
        break
    else:
        print("Invalid option.")

#Dictionaries

#Contact Book
contacts = {
    "Aryan": "6304388018",
    "Ajay": "9391656680"
}

while True:
    print("\nOperations you can perform:" \
          "\n1. Add a contact" \
          "\n2. Remove a contact" \
          "\n3. Modify a contact" \
          "\n4. Show all contacts" \
          "\n5. View a specific contact" \
          "\n6. Exit")

    choice = input("\nEnter your choice: ").strip()

    if choice == "1":
        name = input("Enter contact name: ").title()
        if name in contacts:
            print("Name already exists.")
        else:
            number = input("Enter phone number: ")
            contacts[name] = number
            print("Contact added successfully.")

    elif choice == "2":
        name = input("Enter the contact name to delete: ").title()
        if name not in contacts:
            print("No such contact found.")
        else:
            del contacts[name]
            print("Contact deleted successfully.")

    elif choice == "3":
        name = input("Enter the name to modify: ").title()
        if name not in contacts:
            print("Contact not found.")
        else:
            number = input("Enter the new phone number: ")
            contacts[name] = number
            print("Contact updated successfully.")

    elif choice == "4":
        if len(contacts) == 0:
            print("Contact book is empty.")
        else:
            print("\n--- All Contacts ---")
            for i, (name, number) in enumerate(contacts.items()):
                print(f"{i+1}. {name}: {number}")

    elif choice == "5":
        name = input("Enter the name to view: ").title()
        if name in contacts:
            print(f"{name}: {contacts[name]}")
        else:
            print("No such contact found.")

    elif choice == "6":
        print("Goodbye!")
        break

    else:
        print("Invalid choice. Please try again.")

#File operations
#Sample file ops

import time

while True:
    print("\n--- Notes Manager ---")
    print("1. Add a note")
    print("2. View all notes")
    print("3. Search notes")
    print("4. Delete all notes")
    print("5. Exit")

    choice = input("Enter your choice: ")

    if choice == "1":
        note = input("Enter your note: ")
        with open("notes.txt", "a") as file:
            file.write(note + "\n")
        print("Note added successfully!")
        time.sleep(1)

    elif choice == "2":
        try:
            with open("notes.txt", "r") as file:
                notes = file.readlines()
                if not notes:
                    print("No notes available.")
                else:
                    print("\nYour Notes:")
                    for i, note in enumerate(notes, start=1):
                        print(f"{i}. {note.strip()}")
        except FileNotFoundError:
            print("No notes found yet.")
        time.sleep(1)

    elif choice == "3":
        keyword = input("Enter a keyword to search: ").lower()
        try:
            with open("notes.txt", "r") as file:
                notes = file.readlines()
                found = False
                for note in notes:
                    if keyword in note.lower():
                        print(note.strip())
                        found = True
                if not found:
                    print("No matches found.")
        except FileNotFoundError:
            print("No notes found yet.")
        time.sleep(1)

    elif choice == "4":
        confirm = input("Delete all notes? (y/n): ").lower()
        if confirm == "y":
            open("notes.txt", "w").close()
            print("All notes deleted.")
        else:
            print("Cancelled.")
        time.sleep(1)

    elif choice == "5":
        print("Goodbye!")
        break

    else:
        print("Invalid choice. Try again.")

